export enum StorageEvents {
  StorageInitializing = 'storage.initializing',
  StorageInitialized = 'storage.initialized',
  StorageStarted = 'storage.started',
  StorageStopped = 'storage.stopped',
  ProviderRegistered = 'provider.registered',
  ProviderResolved = 'provider.resolved',
  StorageValidated = 'storage.validated',
  StorageDisposed = 'storage.disposed'
}
